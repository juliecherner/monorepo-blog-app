{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "APP_ENV=local yarn doppler next dev -p 3000",
    "start": "APP_ENV=local yarn doppler next dev -p 3000",
    "build": "next build",
    "lint": "next lint",
    "doppler": "doppler -p app_server -c ${APP_ENV} run -- ",
    "test": "APP_ENV=local yarn doppler next dev"
  },
  "dependencies": {
    "next": "^13.4.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "ui": "*"
  },
  "devDependencies": {
    "@types/node": "20.4.8",
    "@types/react": "18.2.18",
    "@types/react-dom": "^18.0.7",
    "eslint-config-custom": "*",
    "tsconfig": "*",
    "typescript": "5.1.6"
  }
}
